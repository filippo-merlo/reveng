You are controlling an agent in a grid environment.

{{ obs_text }}

Available actions:
  - 0: LEFT
  - 1: RIGHT
  - 2: UP
  - 3: DOWN

Select the best action to reach the goal.
Respond with JSON containing:
  - action: the integer corresponding to the action (0: LEFT, 1: RIGHT, 2: UP, 3: DOWN)
  - confidence: a float between 0 and 1 indicating your confidence that you can reach the goal from your current position

  DO NOT INCLUDE ANY `json` or `jsonb` in your response NOR TICK MARKS LIKE THIS: ```json ```. Start with { and end with } exactly.
